# Composer dependencies — installed locally, embedded into the WP.org build zip.
vendor/
composer.phar

# Strauss — the scoping tool (phar) and its prefixed output. The build
# script regenerates vendor-prefixed/ from vendor/ at package time.
strauss.phar
vendor-prefixed/

# OS / IDE clutter
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log
debug.log

# Build artifacts
node_modules/
*.zip
build/
dist/

# WordPress local environment leftovers
wp-content/
wp-config.php
